# Encargo para Codex · 23/09/2026

Dos tareas independientes en toldos-testar. La primera toca código (un dibujo y un rótulo); la segunda es **solo de lectura**: un informe que después revisará Claude antes de cambiar ninguna regla. Iván revisará también el resultado.

## Reglas de trabajo (obligatorias)

- Trabaja en una rama nueva `codex/antica-dibujo-e-informe-iris-hera` creada desde `main`. **No hagas commit en `main`, no hagas merge y no despliegues.** Nunca trabajes en el servidor 192.168.0.90 (`/webs/toldos-testar`): es producción.
- No arranques la app con el `.env` real: escribe en la carpeta compartida del taller. Para verla, usa la instancia aislada del puerto 4310: `bash .claude/skills/running-toldos-testar/start-isolated.sh` (lee `.claude/skills/running-toldos-testar/SKILL.md`). Comprueba antes que `curl http://127.0.0.1:4310/api/health` dice `"simulationMode":true,"fileWritesEnabled":false`.
- La base de datos RPS (SQL Server, credenciales en `.env`) es **solo de lectura**: solo `SELECT`.
- Los archivos de `src/` usan saltos de línea CRLF o LF según el archivo: respétalo.
- Antes de dar algo por terminado: `pnpm vitest run`, `pnpm lint` y `pnpm build` (el build incluye `tsc --noEmit`) tienen que pasar.
- Mensajes de commit en español, en presente y explicando el porqué. Comentarios de código en español, al estilo de los que ya hay (qué problema resuelven y con qué dato se comprobó).
- Si algo no está claro, **no lo decidas**: anótalo en el informe como pregunta.

## Tarea 1 · Dibujo de Cambio Antica y rótulo de la ventana

1. **F-A01.** En el PDF de tela del Cambio Antica con la variante "TUBO 50x30", el rótulo "ENTRADA TUBO 50x30" se dibuja encima de la línea de la lona y no se lee. Está en `src/domain/planteamientoPdf.js` (busca `ENTRADA TUBO`). Muévelo para que no pise ninguna línea, sin cambiar el resto del dibujo. Para verlo: `tmp/ui-audit/otros-dibujos.mjs` con la instancia aislada, o genera el PDF con `buildOrderPlanteamientoPdf` y mide posiciones con `pdfjs-dist` como hacen los tests de `src/domain/planteamientoPdf.test.js`. Añade un test que compruebe que el rótulo y la línea no se solapan.
2. **Rótulo de la altura de ventana.** En la tarjeta el campo se llama "H. ventana" (`src/client/components/AwningColumn.tsx`), pero el aviso de "FALTA" dice "altura ventana" (`src/domain/awningCompleteness.js`). Que digan lo mismo: el campo pasa a "Altura ventana" si cabe en su columna a 1280×720 (compruébalo con una captura en la instancia aislada); si no cabe, el aviso pasa a decir "H. ventana". Ajusta los tests que dependan del texto.

Entrega: commits en la rama, con capturas antes/después en `tmp/ui-audit/shots/` y una línea en `docs/modelos/antica.md` (apartado de fallos F-A01) diciendo cómo quedó.

## Tarea 2 · Informe de consumo real de Iris y HERA (solo lectura)

Contexto: Iván decidió que todos los modelos reserven lo mismo que se consume de verdad en el almacén (`CPRImputationMaterialMO`), también Iris y HERA, que hoy solo reservan lona y cristal (Iris) o tela y cadena (HERA). Antes de cambiar sus reglas hace falta saber qué se consume. **No cambies `src/`** en esta tarea.

1. Averigua con qué artículos de venta de RPS se venden (`FACOrderLineSL` → `STKArticle.CodArticle`; en `scripts/lib/reservation-gap.mjs` están `IRIS%` y `HERA%`). Lista los artículos, cuántas OF tienen y de qué años.
2. Para cada artículo, saca el consumo por OF desde 2024 agrupado por familia de pieza (sin lacado ni largo) y la cantidad por unidad vendida. Hay un script que lo hace: `node tmp/cofres/por-of.mjs <ARTÍCULO> [regex]` (léelo antes; si `tmp/` no existe en tu copia, escribe uno equivalente en `tmp/`). Separa lo que es estructura (perfiles, soportes, guías, brazos, casquillos, motor y su kit, máquina, manivela, tapas, varillas…) de lo que no (tela, embalaje `TUBOTRA*`/`FILMEMBALAR`, vinilo `V504*`, lacado exterior `EXT_LACAR`, restos `RESTO*`).
3. Ejecuta `pnpm validate:reserva IRIS` y `pnpm validate:reserva HERA` y copia lo que falta y lo que sobra.
4. Para cada pieza de estructura que aparezca en muchas OF, intenta decir de qué depende: la variante (submodelo), motor o máquina, el lacado o el frente. Para el frente, los libros están en `Y:\2025\TOLDOS` y `Y:\2026\TOLDOS` (hojas `ESTR.0n`, modelo en `D6`, frente en `Q11`); `tmp/cofres/anchos.mjs` cruza consumo y frente. Si no hay una regla clara, dilo así: no la inventes.
5. Busca en la intranet (MediaWiki de producción, `http://192.168.0.127/mediawiki/api.php`, solo lectura; `list=search` no funciona, usa `list=allpages&aplimit=500` y luego `prop=revisions&rvprop=content|timestamp|user&titles=<Página>`) las páginas de Iris y HERA y los manuales que enlacen. Anota qué dicen de piezas, límites y cortes.

Entrega: `docs/modelos/informe-iris-hera-consumo.md` con, para cada modelo, una tabla "pieza · código · en cuántas OF · cantidad por toldo · de qué depende (o 'sin regla clara')", lo que falta y sobra según `validate:reserva`, lo que dicen la intranet y los manuales, y una lista de preguntas para Iván. Commit solo de ese archivo en la rama.

## Al terminar

Deja la rama subida (`git push -u origin codex/antica-dibujo-e-informe-iris-hera`) y un resumen corto: qué hiciste, qué comprobaste y qué quedó sin resolver.
